# 검수 메모 — 2026-09-13

범위: 로컬 읽기 전용 초안 도감. 본 게임/런처/원본 JSON 변경 없음. 별도 Godot 엔진 실행·가져오기·게임 저장 없음.

## 정적·API 확인

- `python server.py`: 8계열, 96계열 노드(액티브 32), 28조합 특성, 총124 출력.
- `python -m unittest test_catalog`: **12 tests OK**. 정상 집계, 선행 누락, 자기 순환, 중복 노드, 조합 직업 누락, 예시 링크 누락, DRAFT 훼손, 변형 링크 누락, 계열 조합 누락, 실패 API 422, 허용 경로, 경로 탈출/비허용 파일 404.
- `node --check app.js`: 종료 0.
- 실제 서버 요청: `GET /api/catalog` 200 + total124, `POST /api/catalog` 501(쓰기 미지원), `GET /server.py` 404.
- 테스트용 Python 한 줄 HTTP 검사 명령은 PowerShell 인용 오류로 1회 실패했고, 이후 PowerShell 실제 HTTP 요청으로 대체하여 위 결과를 확인함. 제품/서버 오류로 숨기거나 집계하지 않음.

원본 SHA256:

- mastery: `f25611af60c349ccb3fe5cec64ea29e3aedac7ca99264236cf08b9d4ba8a1acf`
- hybrid: `bd156aab512236ab200c8e6af54262b83f65bfa1281a861bd62cb9fa4ef7f683`

## 실제 브라우저 확인

127.0.0.1:8767, Codex in-app browser의 실제 페이지/접근성 트리/스크린샷 사용. 페이지는 백그라운드에서 검수했으며 게임 창은 열지 않음.

- 8개 계열 버튼을 각각 클릭: 모두 노드12, 연결 조합7, 가로 넘침 false 확인.
- 검술 `끝선 멈춤` 클릭 → 행동 변형/대상 액티브/선행 `간격 베기` 표시 → 선행 링크 클릭하여 실제 노드 상세 이동.
- 제련 조합 카드의 `낮춘 방패` 링크 클릭 → 수호 계열 및 해당 노드 상세로 이동.
- 상단 합계124, DRAFT/본 게임 미적용/열람 전용 배지, SP/비용 미확정 표시 확인.
- 실제 스크린샷을 도구 출력으로 검토·공유. 파일로 저장한 PNG가 있다는 주장은 하지 않음. 양피지 상세판과 시스템 한글 폰트의 일반 브라우저 화면이며 FHD 게임 화면이 아님.

한계: 브라우저의 모든 노드 96개를 각각 클릭하거나 전체 조합28개의 장문을 개별 시각 검수한 것은 아님. 구조 링크는 전체 정적 검사, UI는 대표 링크와 모든 계열 전환을 검사했다. 숫자는 현재 초안 원본 집계일 뿐 밸런스/구현 완료 지표가 아니다. UI 디자인은 검토용이며 게임 최종 디자인 승인이 아니다.

## 독립 검토와 후속 회귀

story 담당이 12개 검사를 독립 재실행했고 필수 보완 0건을 보고했다. 해당 담당은 브라우저 재검수가 불가능하여 UI 실측은 저자/root로 구분한다. 개선으로 발견한 invalid hash 뒤 정상 노드로 이동해도 오류 상태문이 남는 문제는 `app.js`의 공통 정상 요약 함수로 수정했다. 저자가 실제 브라우저에서 오류→검술 버튼→124항목 정상 요약 복원을 확인했고 기존12검사와 JS 구문 검사도 재통과했다.

root 독립 실측 전달(2026-09-13): invalid hash→검술 정상124 summary 복원, 제련→철벽공병 예시의 낮춘 방패 링크 클릭→수호 선택/상세 ID `v3_guard_low_brace` 일치. 전체 96노드 개별 클릭 검사나 게임 내 실행 검사로 확대 해석하지 않는다.

## 12개 빌드 예시 확장 — 2026-09-13

기존12 + 신규7 = **19 unittest PASS**, `node --check app.js` native0. 신규 검사는12/60 집계·runtime/SP 미검증 표시·3개 실파일 SHA 일치, 존재하지 않는 노드, 역순 선행, 초과 슬롯, 잘못된 변형 대상, raw JSON 경로404, 빌드 검증 실패 API422이다. 기존 progression/build validator 코드와 원본3 JSON은 수정하지 않았다.

실제127.0.0.1:8767 브라우저에서12개 버튼 전부 클릭: 각 체크리스트5개, 링크7~8개, 가로 넘침false. 철벽공병 학습 링크→수호의 `v3_guard_low_brace` 상세 이동 확인. invalid hash 상태에서 빌드 학습 링크→`v3_forge_wide_line` 이동 시 정상124 summary 복원 확인. 원본 텍스트는 textContent/createTextNode 방식으로 표시하며 HTML 문자열 삽입이 없다. 실제 UI 스크린샷은 CUA 도구 출력으로 공유했으며 PNG 파일 생성 주장은 하지 않는다. 기존19검사 성공 도구 chunk `7ded76`; 브라우저에서 효과/60개 체크리스트를 실행한 것은 아니다.

기존 서버만 정상 중단하고 같은 localhost8767에서 재시작했으며 다른 프로세스를 종료하지 않았다. source hashes: mastery `f25611af60c349ccb3fe5cec64ea29e3aedac7ca99264236cf08b9d4ba8a1acf`, hybrid `bd156aab512236ab200c8e6af54262b83f65bfa1281a861bd62cb9fa4ef7f683`, build `8eca14a19ab0a7f7c6d9555943a132c31bc05356ed25ff9b6637574504caff29`. 새 이미지·전투·SP소비·게임저장·런타임 연결은 없다. 당시 독립 DA 대기 상태였으며 아래 저녁 재개 후 종결 기록을 우선한다.

root 독립 브라우저 검수 전달: 기존 탭 새로고침 후12빌드 목록, 보루궁수의5개 체크리스트와 충격 흘리기 후퇴 문구를 확인했다. 학습 순서의 고요한 시위 클릭으로 궁술 계열/정확한 노드 상세가 선택됐고 새 빌드 화면 스크린샷을 도구 출력으로 공유했다. root는 server.py/app.js의 읽기 전용·3JSON·textContent도 직접 확인했다. 이는 전체 전투 효과나 모든 빌드 체크리스트 실행 검증이 아니다.

저녁 재개 후 독립 DA 종결: art_review 담당이 19 unittest를 재실행하여 PASS/native0(`d394a0`), JS 구문 native0(`81817f`), 원본3JSON 전후 SHA 불변을 확인했다. 허용 GET4개·그 외404·POST 미구현·localhost/CSP·텍스트 노드 표시·학습 링크의 계열 선택을 코드 검토했고 필수 재작업0으로 판정했다. 이번 독립 검토는 코드/테스트이며 실제 브라우저 조작은 위 저자/root 기록에 한정한다.
