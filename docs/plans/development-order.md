# 개발 순서 (바이브 코딩 6단계 가이드의 프로젝트 적용판)

> 2026-09-18 확정. 사용자가 제시한 "비개발자 1인 개발 6단계" 가이드를 **현재 진행 상황에 맞게 재배치**한 것이다.
> 가이드는 빈 폴더에서 시작하는 순서이고, 이 프로젝트는 이미 1~5단계를 지났다. 그대로 따르면 되돌리는
> 작업이 되므로 **원칙은 채택하고 순서만 옮긴다.** 관련 결정: `docs/brd/04-decisions.md` D-145.

## 0. 가이드 6단계 ↔ 현재 상태 대조

| 가이드 단계 | 가이드가 권하는 것 | 이 프로젝트의 현재 | 판단 |
|---|---|---|---|
| 1. 엔진 선택 | PhaserJS(웹) 또는 Godot | **Godot 4.4.1 확정**(GDScript, 헤드리스 검증 3단계) | **유지.** 웹 실행은 Godot 웹 내보내기로 이미 확보(`docs/qa/web-demo.md`). PhaserJS 전환은 전투·데이터·테스트 전부 재작성이라 기각 |
| 2. 컨텍스트 구축 | `CLAUDE.md`에 규칙 | `CLAUDE.md` + `.claude/agents/`(12명) + `docs/brd/04-decisions.md`(D-01~) | **유지·보강.** 아래 §2의 규칙 3개를 추가 |
| 3. 핵심 동사 MVP | 이동 + Space 공격 + 상자 파괴, 사각형 placeholder | M1: 이동·3타 콤보·구르기·가드·몬스터 3종·히트스톱 완료, 사용자 플레이테스트 3라운드 | **완료.** 다만 "한 번에 동사 하나"라는 원칙은 이후 단계에 그대로 적용 |
| 4. 애셋 연동·인덱싱 | `assets/` 폴더 + `assets.json` 목록 | CC0 임시 애셋 연동 완료, 원작 아트는 D-140(핀 64×96) 제작 중. **애셋 인덱스 파일은 없음** | **부분 완료.** `assets.json` 인덱스를 추가한다(§1-①) |
| 5. RPG 시스템 하나씩 | 몬스터 AI → 데이터 분리 → HUD/레벨업 | 몬스터 AI·정예, `game/data/*.json` 데이터 분리, HUD, 인벤토리·대장간·세이브·퀘스트 완료. **레벨업/경험치 팝업은 미구현** | **대부분 완료.** 남은 항목(경험치·레벨업, 스킬 슬롯, 퀘스트 로그 UI)을 §1 순서로 |
| 6. 테스트·디버깅·배포 | 콘솔 에러 증거, 테스트 출력 요구, Vercel 배포 | GUT 387·스모크 32·헤드리스 검증 출력을 PR 본문에 기재, GitHub Pages 배포 | **유지.** Vercel 대신 GitHub Pages(동등) |

## 1. 앞으로의 순서

각 항목은 "동사 하나 = 단계 하나 = PR 하나"다. 단계마다 ① 계획(무엇을·어느 파일을·어떤 검증으로) → ② 구현 → ③ 헤드리스 검증 출력 첨부 → ④ PR → ⑤ 웹 데모 재배포 → ⑥ 사용자 플레이 소감 순으로 돈다.

| # | 단계 | 동사 | 완료 기준 | 담당 |
|---|---|---|---|---|
| ① | 애셋 인덱스 | "무엇이 있는지 안다" | `game/assets/assets.json`(경로·규격·출처·라이선스 등급) 자동 생성 스크립트 + `validate_tables.py` 연동. 참조되지 않는 애셋과 미등록 애셋을 목록화 | asset-wrangler |
| ② | 경험치·레벨업 | "적을 잡으면 강해진다" | `stats.json` 곡선으로 경험치 획득 → 레벨업 팝업·스탯 상승·HUD 갱신. 가이드 5-③ 항목 | game-designer → godot-engineer |
| ③ | 스킬 슬롯 1·2 | "Q/R로 기술을 쓴다" | 캐릭터 고유 메커니즘 중 핀 1종(패리 슬래시)만. GDD F2-5 | godot-engineer |
| ④ | 퀘스트 로그 UI | "무엇을 해야 하는지 본다" | 메인/사이드/의뢰 탭, 추적 전환. F5-1 | ui-ux-designer |
| ⑤ | 원작 아트 1차 교체 | "핀이 진짜 얼굴을 갖는다" | D-140 64×96 정면·걷기 승인 후 `Player.tscn` 교체, FHD 카메라·타일 규격 재검토(D-144 재검토 항목) | pixel-artist → godot-engineer |
| ⑥ | 대사 시스템 | "NPC와 대화한다" | Dialogue Manager 연결, 토스트 대체, 몽실이 분기 선택 UI | narrative-writer → godot-engineer |
| ⑦ | 게임성 게이트 2차 | "재미있는가" | 웹 데모로 사용자 플레이 → 타격감·성장 체감·목표 명확성 3축 소감 → 우선순위 재조정 | 사용자 |

②~④는 서로 독립이라 병렬 진행 가능. ⑤는 아트 승인에, ⑥은 ④에 의존한다.

## 2. 가이드에서 새로 채택하는 규칙 3개

1. **계획 먼저.** 단계 착수 시 엔지니어에게 바로 구현을 시키지 않고, 수정할 파일·순서·검증 방법을 먼저 적게 하고 디렉터가 검토한 뒤 구현한다(가이드 3단계 `/plan` 원칙). 지금까지는 지시문에 파일을 명시하는 방식이었고, 앞으로는 에이전트의 계획 회신을 한 번 거친다.
2. **증거 없는 "고쳤습니다" 금지.** 완료 보고에는 실제 테스트 출력(GUT 수치·스모크 결과·콘솔 오류 수)을 반드시 붙인다. 이미 관례지만 규칙으로 명문화(가이드 6단계).
3. **파일당 500줄 상한.** 넘는 스크립트는 순수 계산(`*_calc.gd`)과 노드 로직으로 분리한다. 현재 `monster_base.gd`(약 830줄)·`inventory_menu.gd`가 초과 — ②~④ 진행 중 손대는 시점에 분리한다.

## 3. 채택하지 않는 것

- **PhaserJS 전환**: 위 표 1행.
- **사각형 placeholder로 되돌리기**: 전투 검증은 이미 실제 스프라이트로 끝났다.
- **Vercel**: GitHub Pages로 같은 결과. 필요하면 나중에 병행.
