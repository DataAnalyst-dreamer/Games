# MQ01 actual keyboard walking — second attempt

2026-09-13. **PASS for this headless input/walking/autosave slice**, not launcher ready or full-game completion. [Failed first attempt](walk-mq01-first-attempt-20260913.md) remains preserved; an out-of-range cargo approach is the leading explanation, but V1 did not record overlap so that cause is not proven directly.

V2 sources: `tools/qa/walk-mq01/smoke_walk_mq01_v2.gd`, `SmokeWalkMq01V2.tscn`; only diagnostic copy receives them. Production unchanged. Natural walking cargo target changed from(-399,190) to source object position(-390,175); script now requires cargo.overlaps_body(player)=true and teo=false before E and logs current objective. No position/monster/invulnerability/quest-state writes. Correct existing monster group (`monster`, MonsterBase line186) reports10 instances at start, replacing V1's wrong plural-group diagnostic.

Same dedicated diagnostic copy and process-local user-data directory, separate no-autoload probe native0 before execution. Test guard verifies exact directory and refuses preexisting slot0_auto. First failed run created no completion save. Normal autosave subscriptions retained. No existing save deletion or overwriting retry.

Observed route (60 fixed simulation fps):

- Main spawn(0,0), monsters10. Slime attack and goblin whistle occurred; no damage or death recorded.
- Waypoint(-300,0) actual(-295.9999,0), walking frames223.
- Teo approach(-350,135) actual(-347.8545,131.8546), frames339. E/Enter accepts; E/Escape talks/closes.
- Cargo approach actual(-387.4525,171.4527), frames382. cargo overlap=true, teo=false, objective1. E changes to complete_ready/objective2.
- Return Teo actual(-353.5113,137.5115), frames419. E/Enter completes MQ01, exp5, one successful auto save and actual slot0_auto file exists.
- `WALK_MQ01_RESULT PASS frames=419 saves=1 exp=5`, **native exit0** in actual transcript. Walking duration419/60≈6.98 simulated seconds, not real human play time; GUI/UI waits are outside that counter. Full timeout bound applies walking frames only.

Assertion distinction: the success condition directly checks no death, MQ01 completed, one successful autosave and file existence. EXP5 is printed observation, not an explicit equality assertion; zero hits/damage are Metrics observations, not direct test assertions. Do not describe these observed values as independently asserted pass conditions.

Logs in workspace `reviews/games-2026-09-12/runtime/autoload-exit-diag-monsters-v1/`: `probe-before-walk-v2.log`, `walk-mq01-v2.log`, `walk-v2-native-transcript.log`. No GUI capture or cross-process save restore in this test. Previous editor import teardown remains blocked; no ready.json created. Existing certificate warning and LUK/INT data warnings remain.

## Post-run source comparison

Read-only SHA256 comparison of current game source files against diagnostic copy, excluding `.godot` and intentionally changed project.godot: **7656 match, 2 differ**. Differences are default_bus_layout.tres (linewise Compare-Object no difference, byte-level formatting differs) and generated localization/ui_ko.ko.translation (regenerated by copy import). Original CSV and all source scripts matched. Do not call this an exact all-byte identical snapshot. Config preserves original autoloads, disables editor plugins and isolates userdata; these are deliberate copy-only deviations.

Independent DA pending. Successful second approach does not erase first failure or prove all possible navigation routes, combat survival, full quest chain or FHD rendering.
