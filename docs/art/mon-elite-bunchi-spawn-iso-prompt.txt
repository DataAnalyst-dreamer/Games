STYLE: 16-bit style pixel art for a cozy medieval fantasy island RPG. TRUE ISOMETRIC view: 2:1 dimetric projection on a 45-degree diamond grid. Orthographic, NO perspective convergence, NO vanishing point. Light comes from the top-left. Crisp hard-edged pixels, NO anti-aliasing, NO blur, NO gradients, NO glow. Flat shading with exactly 3 tones per surface. Cute, sad-but-not-scary proportions, warm and hand-made feel. No text, no watermark, no signature, no UI. Transparent background.

CHARACTER IDENTITY (D-227 — reuse the slime skeleton, `docs/art/mon-slime-iso-prompt.txt`, NOT a new creature): SAME round jelly silhouette, SAME body proportions, SAME flattened-bottom ground contact, SAME two-pose set (FRONT/S and RIGHT-side/E only — this creature is radially near-symmetric like the base slime, no full 8-direction set needed). This is a palette-swap variant of the base slime, matching `game/data/monsters.json`'s `elite_base_monster_id: "slime"` and `on_death_split_monster_id: "slime"` (defeating it splits into 2 ordinary slimes, so the family resemblance must be obvious at a glance).

Differences from the base slime, "뭉치의 새끼" (child of Bunchi), an ash-tainted elite spawned by the region's corrupted slime-king boss:
- Dull grey-plum jelly instead of clear teal: base color a muted grey-plum (#4e484a mixed lightly with the base slime's #79b8ce, keep it recognizably jelly-like, not solid), with dark ash-grey blotches (#4e484a) patched unevenly across the body like soot or scarring, and a faint purple undertone (#a26a83) at the rim instead of the base slime's bright teal highlight.
- Two dim, pale glowing eyes (#d4f5fa), slightly sadder/droopier than the base slime's happy round eyes, hinting the gentle creature underneath is still there (world lore: this is a fragment of a once-tame pet slime, not a monster to be feared).
- Slightly larger and lumpier silhouette than the base slime canvas (still the same 32x32 logical canvas and proportions — elite scale-up beyond this is handled by the engine's existing 1.4x runtime scale, not by drawing a bigger canvas).
- No spikes, no teeth, no aggressive shapes — this game never shows scary or grotesque silhouettes even for "corrupted" enemies.

Deliver two separate 1024x1024 images: (1) FRONT view facing the camera (S); (2) RIGHT side view facing east (E). Both at the same scale as the base slime prompt so the family resemblance is obvious side by side.
