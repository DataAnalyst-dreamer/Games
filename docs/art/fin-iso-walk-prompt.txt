Create a PRODUCTION GAME SPRITE SHEET, using the attached five approved isometric Fin poses (S, SW, W, NW, N — from fin-iso-8dir-prompt.txt) ONLY as character identity and pose reference for those five directions. This is not a concept board. Exact requested output: transparent RGBA PNG, EXACT uniform grid of 8 columns and 5 rows, each cell 192x256 pixels (1536 pixels wide by 1280 pixels tall). No grid lines, no labels, no shadows on ground, no text, no props or background. 40 sprites total, one centered complete character inside each cell, no missing cells. Every character same scale, feet ground line at the same fixed y within every cell, centered x, all weapons fit inside the cell. TRUE ISOMETRIC 2:1 dimetric projection, 45-degree diamond grid, orthographic, no perspective convergence, light from the top-left. Render in precise chunky PIXEL ART with a logical pixel grid: the character body is about 64x96 logical pixels on a padded 96x128 logical canvas per cell (matching the reference poses' proportions exactly), hard edges, no antialiasing or soft blur, simple deliberate cluster shading, dark ink outline (not pure black). Identity: Fin, a cute 2.5-head apprentice knight, open silver helmet with small coral-red feather, visible light brown hair, ochre tunic, short sage-green cape, brown boots, short sword ALWAYS in HIS RIGHT hand, small round wooden shield ALWAYS in HIS LEFT hand. Exact row contents, one direction per row, do not rotate direction within a row:
ROW 1 = walking S (facing camera), full face visible throughout.
ROW 2 = walking SW, shield forward on the near side throughout.
ROW 3 = walking W (full left profile), shield edge-on near arm throughout.
ROW 4 = walking NW, cape visible over the back, only a sliver of face if any.
ROW 5 = walking N (facing away), face never visible, cape and back of helmet throughout.
Eight columns within each row = a sequential looping WALK cycle of 8 frames matching this exact key-pose order: 0 left foot contact, 1 weight settling onto left leg (body dips slightly), 2 right leg passing, 3 right foot forward reaching, 4 right foot contact, 5 weight settling onto right leg (body dips slightly), 6 left leg passing, 7 left foot forward reaching (loops back to frame 0). Consistent face, helmet and equipment across all 8 columns of a row; only legs alternate and torso does a small 2-4px bob — do NOT change equipment size, character scale or head shape between columns (this exact defect got walk v1-v3 rejected before: sword swinging wildly, body rotating sideways, silhouette narrowing, and PixelLab returning fewer frames than requested). Each cell is independent with empty transparent margins, no adjacent overlap, no repeated identical pose copy-pasted across columns — legs must visibly alternate every frame while torso orientation stays fixed per row. Pixel-art production sprite atlas, not a detailed illustration. Reference image poses, lettering, environment and background must NOT appear in the output.
